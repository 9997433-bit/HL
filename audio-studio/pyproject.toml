[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "audio-studio"
version = "1.1.0"
description = "Professional audio editing and analysis workstation (Adobe Audition class MVP)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Audio Studio Team" }]
keywords = ["audio", "dsp", "daw", "waveform", "spectrum", "editor"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: X11 Applications :: Qt",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Sound/Audio :: Editors",
]

dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "soundfile>=0.12.1",
    "PySide6-Essentials>=6.6",
]

[project.optional-dependencies]
audio = ["sounddevice>=0.4.6", "PyAudio>=0.2.13"]
mastering = ["soxr>=1.1.0"]
# GPL-3.0 boundary: installing this extra pulls in pedalboard (GPL-3.0 with
# JUCE/Rubber Band/FFTW). It is opt-in only and must never be bundled into an
# MIT-licensed binary artifact. See THIRD_PARTY_LICENSES.md.
plugins = ["pedalboard>=0.9.0"]
# Build-time only: PyInstaller is GPL-2.0-or-later with a bootloader exception
# that covers shipping the bootloader inside this MIT application. It is never
# a runtime dependency of the bundle it produces.
installer = ["pyinstaller>=6.3"]
dev = [
    "pytest>=7.4",
    "pytest-qt>=4.2",
    "mypy>=1.8",
    "ruff>=0.3",
]

[project.scripts]
audio-studio = "audio_studio.app:main"
audio-studio-batch = "audio_studio.batch.cli:main"

[project.gui-scripts]
audio-studio-gui = "audio_studio.app:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["audio_studio*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
filterwarnings = ["ignore::DeprecationWarning"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "C4", "SIM"]

[tool.mypy]
python_version = "3.10"
warn_unused_ignores = true
disallow_untyped_defs = true
ignore_missing_imports = true
