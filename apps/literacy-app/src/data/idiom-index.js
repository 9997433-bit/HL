/**
 * 成语语料的「轻」索引。
 *
 * 首页的学习地图只想在卡片上写一句「学过 3 / 60」，为这一个数字把整份
 * idioms.js（六十条成语，连故事带情景题）拉进首页的分块并不划算。
 * 这里只留数字，真正的语料仍然由成语列表页 / 详情页按需加载。
 *
 * 这个数字必须和 IDIOMS.length 一致，`npm run check:data` 会核对。
 */

export const TOTAL_IDIOMS = 60
