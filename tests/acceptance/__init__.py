"""Acceptance-criteria registry and (future) acceptance test suites.

The registry in ``test_criteria_registry.py`` is the single machine-readable
source of truth for acceptance criteria IDs defined in
``docs/ACCEPTANCE_CRITERIA.md``.
"""
